#include<bits/stdc++.h>
using namespace std;
int main(){
    unordered_set<int> us; //only uniqueue not in ordered
    us.insert(10);
    us.insert(20);
    us.insert(5);
    us.insert(10);  //{} in random rest same as set
    
    return 0;
}